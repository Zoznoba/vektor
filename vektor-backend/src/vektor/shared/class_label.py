"""Подпись класса: «8-1», «10».

Появилась под импорт всей школы: 10 и 11 классы параллелей не имеют, секция у
них пустая, а собиралась подпись как `f"{grade}-{section}"` в ЧЕТЫРЁХ местах
бэкенда — и давала «10-» прямо в UI.

ОСТОРОЖНО, расхождение с фронтом. Фронтовый classLabel
(vektor-frontend/src/types/school.ts) пишет НЕЧИСЛОВУЮ секцию слитно («7к»), а
эта функция сохраняет дефис («7-к») — так бэкенд вёл себя всегда, и менять
это заодно с импортом не стали: буквенных секций в данных школы нет, а
поведение видимое. Расхождение предсуществующее, не следствие этой правки;
чинить его — отдельным решением, вместе с тем, какой вариант школе нужен.
"""


def class_label(grade: int, section: str | None) -> str:
    if not section:
        return str(grade)
    return f"{grade}-{section}"
