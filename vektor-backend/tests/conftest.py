# Общие фикстуры pytest.
#
# На Этапе 0 здесь пусто. В Этапе 2 (auth + users) сюда приедут:
#   - фикстура тестовой БД (отдельная схема / транзакция на тест)
#   - переопределение зависимости get_db на тестовую сессию
#   - AsyncClient поверх ASGI-приложения
