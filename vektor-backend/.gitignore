# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# uv / venv
.venv/
# Отдельный venv для проверок из Windows-инструментов (Claude Code) — не
# трогает .venv, которым пользуется WSL (make run), чтобы платформа venv
# не переключалась туда-сюда при каждом запуске с той или другой стороны.
.venv-winqa/

# Тесты и линтеры
.pytest_cache/
.ruff_cache/

# Локальное окружение
.env

# IDE
.vscode/
.idea/

# Контекст Claude Code — локальный, в репозиторий не коммитим
CLAUDE.md
